# 

name: CI

# 
on:
  # 
  push:
    branches: [ main ]
  workflow_dispatch:
  schedule:
    #- cron: '*/10 * * * *'
    - cron: '*/1 * * * *'

  pull_request:
    branches: [ main ]

  # 

# 
jobs:
  # 
  build:
    # 
    runs-on: ubuntu-latest

    # 
    steps:
      # 
      - uses: actions/checkout@v2

      # 
      - name: first commit
        run: date >> empty.md

      # 
      - name: second commit
        run: |
          git add .
          git config user.name 'yasserbdj96'
          git config user.email 'by.root96@gmail.com'
          git commit -m "empty"
          git push 
          
      - name: third commit
        run: sed -i '$ d' empty.md
        
      - name: finishing
        run: |
          git add .
          git config user.name 'yasserbdj96'
          git config user.email 'by.root96@gmail.com'
          git commit -m "empty"
          git push 
